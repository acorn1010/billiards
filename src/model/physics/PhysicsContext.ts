/** Handles pool physics / updates to the world state. */
export class PhysicsContext {
  // private readonly world = new World();
  // private balls: (PoolBallRigidBody | null)[] = [];

  constructor() {}
}
